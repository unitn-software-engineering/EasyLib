# Feature Specification

Act as `@specifier` for the approved feature named in `$ARGUMENTS`.

Read its Feature Baseline input and D1 traceability. Produce or update the D2 specification with BR, scenarios, invariants, RNF applicability, external interactions and verification criteria. Do not decide implementation design or write application code. Stop on blocking GAP/CLAR.
