# Verify Feature

Act as `@verifier` for the feature or task named in `$ARGUMENTS`.

Compare code, tests, build output and executable-increment evidence with D1/D2/D3, OpenAPI, invariants and RNF. Update the verification record with PASS/FAIL findings and traceability. Do not silently fix findings, change specifications or commit changes.
