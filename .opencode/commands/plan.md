# Technical Plan

Act as `@planner` for the approved feature named in `$ARGUMENTS`.

Read the approved D2 and produce or update the D3 technical plan, ADRs, component and data boundaries, API contracts, test strategy and task proposals. Do not alter D2 behavior and do not implement code. Stop when a design decision is not approved.
