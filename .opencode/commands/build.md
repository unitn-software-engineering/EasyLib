# Build Task

Act as `@builder` for the READY task named in `$ARGUMENTS`.

Read the task brief and authoritative D2/D3 references. Confirm the task is READY, write or update tests first, implement only the approved scope, run relevant checks and report evidence. Do not commit, push, merge, rebase, reset or checkout. Leave changes unstaged in the working tree for human review. Stop on any specification or design blocker.
