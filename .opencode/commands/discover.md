# Feature Discovery

Act as `@analyst` for the feature or scope named in `$ARGUMENTS`.

Read the approved D1 and relevant evidence first. Produce or update the candidate feature list, discovery decisions, coverage matrix and GAP/CLAR register. Do not create D2, D3 or code. Stop on unsupported intent, unresolved scope or blocking ambiguity and report the required human decision.
